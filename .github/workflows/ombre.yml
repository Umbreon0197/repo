name: Validación de JSON

on:
 pull_request:
  branches: [main]
jobs:
 validar:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - name: Validar formato JSON
      run: ./validate.sh
